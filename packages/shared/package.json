{
  "name": "@agentic/shared",
  "version": "0.1.0",
  "description": "Shared types, schemas, and constants for the Agentic SRE platform.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --reporter=basic",
    "test:coverage": "vitest run --reporter=basic --coverage"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
