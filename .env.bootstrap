# Bootstrap environment variables — only these 3 are needed to start
# Everything else is configured via the dashboard setup wizard
POSTGRES_URL=postgresql://agentic:agentic@agentic-postgres:5432/agentic
TEMPORAL_ADDRESS=temporal:7233
ENCRYPTION_KEY=
